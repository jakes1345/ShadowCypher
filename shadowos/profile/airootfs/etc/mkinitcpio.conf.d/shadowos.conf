# ShadowOS mkinitcpio overlay — adds plymouth hook so the boot splash
# actually appears. Lands in /etc/mkinitcpio.conf.d/ which is auto-merged.
HOOKS=(base systemd autodetect microcode modconf kms keyboard sd-vconsole block plymouth filesystems fsck)

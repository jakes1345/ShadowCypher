---
# Calamares unpackfs — tells the installer where the live rootfs lives.
# archiso mounts the running rootfs at /run/archiso/airootfs (dir type).
# We copy it verbatim to the partition mounted at "".

unpack:
    - source: /run/archiso/airootfs
      sourcefs: dir
      destination: ""

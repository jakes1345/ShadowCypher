---
# Force sddm as the display manager for the installed system.
# Without this, Calamares auto-detects which is unreliable on live ISOs
# that have multiple DMs partially configured.

displaymanagers:
    - sddm

basicSetup: false

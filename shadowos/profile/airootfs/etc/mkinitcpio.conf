MODULES=(iso9660 squashfs loop overlay)
BINARIES=()
FILES=()
# Live-boot hooks: archiso finds+mounts the ISO squashfs via label/UUID.
# autodetect/block/filesystems/fsck are for installed systems, not live media.
# customize_airootfs.sh inserts 'plymouth' after 'udev' at build time.
HOOKS=(base udev microcode modconf kms block keyboard keymap consolefont archiso)

# ShadowOS Process Resource Limits

# Prevent fork bombs
* soft nproc 1024
* hard nproc 1024

# Memory limits
* soft memlock unlimited
* hard memlock unlimited

# File limits
* soft nofile 65535
* hard nofile 65535

# Core dumps (disabled for security)
* soft core 0
* hard core 0

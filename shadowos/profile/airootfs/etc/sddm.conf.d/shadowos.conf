[Theme]
Current=shadowos
CursorTheme=Adwaita
Font=JetBrainsMono Nerd Font

[Wayland]
SessionDir=/usr/share/wayland-sessions

[General]
DisplayServer=wayland
GreeterEnvironment=QT_WAYLAND_SHELL_INTEGRATION=layer-shell

# Autologin disabled — live ISO requires manual login for security.
# Default credentials are shadow/shadow; change immediately on first boot.
# [Autologin]
# User=shadow
# Session=hyprland.desktop
# Relogin=false

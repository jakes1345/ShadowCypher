import { shortest } from "@antiwork/shortest";

// Services
shortest("Log in with 'admin'/'shadow', click 'Services' in the sidebar, and verify the page shows Minecraft Server status, Docker containers section, Tailscale VPN section, and Systemd Services table");

shortest("On the Services page, verify the Systemd Services table shows at least 5 services with their active/inactive status");

import { shortest } from "@antiwork/shortest";

// AI Assistant
shortest("Log in with 'admin'/'shadow', click 'AI Assistant' in the sidebar, and verify the page shows a chat interface with a message input, Send button, mic button, and Config button");

shortest("On the AI Assistant page, click '⚙️ Config' and verify a modal appears with Personality dropdown (JARVIS/FRIDAY/Neutral), Provider dropdown, API Key field, and a Speak replies checkbox");

shortest("On the AI Assistant page, verify the heading shows 'JARVIS' (the default personality name)");
